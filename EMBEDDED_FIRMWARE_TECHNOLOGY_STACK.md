# Remote-Env-Monitor 下位机技术栈说明

**项目名称：** Remote-Env-Monitor（远程环境监测系统）
**下位机芯片：** STM32F407ZGT6
**操作系统：** FreeRTOS
**通信协议：** Modbus RTU (Freemodbus)
**文档版本：** 1.0
**完成日期：** 2026年2月18日

---

## 📋 目录

1. [系统架构概述](#系统架构概述)
2. [硬件平台](#硬件平台)
3. [软件栈](#软件栈)
4. [核心模块说明](#核心模块说明)
5. [数据流和通信](#数据流和通信)
6. [传感器驱动](#传感器驱动)
7. [任务调度](#任务调度)
8. [资源和性能](#资源和性能)
9. [开发指南](#开发指南)

---

## 系统架构概述

### 整体系统设计

```
┌─────────────────────────────────────────────────────────┐
│  Remote-Env-Monitor 完整系统架构                         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  上位机（i.MX6ULL Linux）                              │
│  ┌─────────────────────────────────────────────────┐  │
│  │ LVGL UI 应用 + libmodbus Master                │  │
│  │ - 1秒更新周期                                   │  │
│  │ - 实时数据显示                                  │  │
│  │ - Modbus RTU 主站通讯                          │  │
│  └─────────────────────────────────────────────────┘  │
│           ↑                                            │
│           │ UART (Serial port)                        │
│           │ 115200, 8N1                               │
│           │ Protocol: Modbus RTU                      │
│           │ Function Code: 03 (Read Holding Reg)      │
│           ↓                                            │
│  ┌─────────────────────────────────────────────────┐  │
│  │  下位机（STM32F407ZGT6 嵌入式系统）             │  │
│  ├─────────────────────────────────────────────────┤  │
│  │  ┌─ FreeRTOS 内核                              │  │
│  │  │  ├─ 传感器采集任务 (100ms 周期)            │  │
│  │  │  ├─ 数据处理任务 (200ms 周期)              │  │
│  │  │  ├─ Modbus 从机任务 (事件驱动)             │  │
│  │  │  └─ 监控任务 (1s 周期)                     │  │
│  │  │                                             │  │
│  │  ├─ Modbus RTU 从机 (Freemodbus)             │  │
│  │  │  └─ 寄存器映射：                          │  │
│  │  │     Reg 0-6: 7个传感器数据                │  │
│  │  │                                             │  │
│  │  ├─ 传感器驱动                                │  │
│  │  │  ├─ AHT10 (I2C) - 温湿度                 │  │
│  │  │  ├─ BMP280 (I2C) - 温压                  │  │
│  │  │  ├─ SGP30 (I2C) - 空气质量               │  │
│  │  │  └─ 其他传感器                           │  │
│  │  │                                             │  │
│  │  └─ HAL 驱动层                                │  │
│  │     ├─ UART (Modbus 通讯)                    │  │
│  │     ├─ I2C (传感器通讯)                      │  │
│  │     ├─ SPI (可选)                            │  │
│  │     └─ GPIO (LED, 按钮等)                    │  │
│  └─────────────────────────────────────────────────┘  │
│                                                         │
│  传感器模块（从机设备）                               │
│  ┌─────────────────────────────────────────────────┐  │
│  │ I2C 传感器：                                    │  │
│  │ - AHT10 温湿度传感器 (I2C Addr: 0x70)        │  │
│  │ - BMP280 气压传感器 (I2C Addr: 0x77)         │  │
│  │ - SGP30 空气质量 (I2C Addr: 0x58)            │  │
│  └─────────────────────────────────────────────────┘  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 数据流向

```
┌──────────────┐
│  传感器数据   │ (模拟信号或 I2C)
└──────┬───────┘
       │
       ↓
┌──────────────────────────┐
│  STM32F407 采集          │
│  - 原始数据读取          │
│  - 模数转换 (ADC)        │
│  - I2C/SPI 通讯          │
└──────┬───────────────────┘
       │
       ↓
┌──────────────────────────┐
│  FreeRTOS 任务处理       │
│  - 数据滤波处理          │
│  - 单位转换              │
│  - 数据格式化            │
└──────┬───────────────────┘
       │
       ↓
┌──────────────────────────┐
│  Freemodbus 从机         │
│  - 寄存器映射            │
│  - Modbus 协议处理       │
│  - 上位机查询响应        │
└──────┬───────────────────┘
       │
       ↓ UART (Serial)
┌──────────────────────────┐
│  上位机 (libmodbus)      │
│  - 1秒周期轮询           │
│  - 数据解析              │
│  - LVGL 显示             │
└──────────────────────────┘
```

---

## 硬件平台

### STM32F407ZGT6 微控制器

```
芯片信息：
  ├─ 型号：STM32F407ZGT6
  ├─ 核心：ARM Cortex-M4F (168 MHz)
  ├─ 浮点运算：支持单精度 FPU
  └─ 架构：哈佛结构 (分离指令/数据总线)

内存配置：
  ├─ Flash 闪存：1MB (用于程序)
  │  └─ 典型分配：
  │     ├─ 引导程序：32KB
  │     ├─ FreeRTOS 内核 + 应用：600KB
  │     ├─ Freemodbus：80KB
  │     ├─ 驱动和库：150KB
  │     └─ 空闲用于扩展：138KB
  │
  ├─ SRAM 内存：192KB (用于运行时)
  │  └─ 典型分配：
  │     ├─ FreeRTOS 堆：80KB
  │     ├─ 任务栈：40KB (5个任务 × 8KB)
  │     ├─ 传感器缓冲：10KB
  │     ├─ Modbus 通讯缓冲：8KB
  │     └─ 其他数据结构：54KB
  │
  └─ CCM (核心耦合内存)：64KB
     └─ 用于高速访问数据 (可选)

时钟配置：
  ├─ 外部晶振：8MHz
  ├─ PLL 倍频：×21
  ├─ 系统时钟：168MHz
  ├─ AHB 时钟：168MHz
  ├─ APB1 时钟：42MHz (UART1, SPI1, I2C1 等)
  └─ APB2 时钟：84MHz (UART, SPI 等)

关键外设：
  ├─ UART (5 个)
  │  └─ UART1: Modbus RTU 通讯 (115200, 8N1)
  │  └─ 其他 UART: 调试、扩展通讯
  │
  ├─ I2C (3 个)
  │  └─ I2C1: 传感器连接 (AHT10, BMP280, SGP30)
  │  └─ I2C2/3: 可扩展
  │
  ├─ SPI (3 个)
  │  └─ 可用于其他传感器或高速通讯
  │
  ├─ ADC (3 个，18个通道)
  │  └─ 用于模拟传感器采集 (可选)
  │
  ├─ 定时器 (12 个)
  │  └─ 用于精确时序和 PWM
  │
  ├─ GPIO (144 个引脚中的 112 个可用)
  │  └─ LED、按钮、状态指示灯
  │
  └─ DMA 控制器
     └─ 用于高效的数据传输 (UART、SPI、I2C)
```

### 典型原理图连接

```
STM32F407
├── UART1 (PA9/PA10)
│   └─→ USB-UART 转换器 ──→ 上位机 (Modbus Master)
│       115200, 8N1, N流控
│
├── I2C1 (PB6/PB7)
│   ├─→ AHT10 (0x70) - 温湿度
│   ├─→ BMP280 (0x77) - 气压/温度
│   └─→ SGP30 (0x58) - 空气质量
│
├── GPIO (LED 指示)
│   ├─→ LED1 (运行指示)
│   ├─→ LED2 (Modbus 活动)
│   └─→ LED3 (错误指示)
│
└── 电源
    └─→ 3.3V (传感器和 MCU)
        5V (USB/调试器)
```

---

## 软件栈

### 软件层级结构

```
┌───────────────────────────────────────┐
│  应用层                                 │
│  ├─ 主程序 (main.c)                   │
│  ├─ 初始化函数                        │
│  ├─ FreeRTOS 任务定义                 │
│  └─ 传感器采集逻辑                    │
├───────────────────────────────────────┤
│  Modbus 从机层                        │
│  ├─ Freemodbus 3.2                   │
│  ├─ 协议处理 (RTU 模式)              │
│  ├─ 寄存器映射 (coil/holding)        │
│  └─ 通讯驱动接口                     │
├───────────────────────────────────────┤
│  操作系统层 (RTOS)                    │
│  ├─ FreeRTOS Kernel (v11.x)          │
│  ├─ 任务管理                          │
│  ├─ 信号量/互斥量                    │
│  ├─ 队列通讯                          │
│  └─ 中断管理                          │
├───────────────────────────────────────┤
│  驱动层 (HAL/LL)                     │
│  ├─ STM32F4xx Standard Perph Lib     │
│  ├─ UART 驱动 (中断/DMA)             │
│  ├─ I2C 驱动                         │
│  ├─ GPIO 驱动                        │
│  ├─ 时定时器驱动                     │
│  └─ 其他外设驱动                     │
├───────────────────────────────────────┤
│  传感器驱动层                          │
│  ├─ AHT10 驱动 (I2C)                 │
│  ├─ BMP280 驱动 (I2C)                │
│  ├─ SGP30 驱动 (I2C)                 │
│  └─ 驱动通用接口                     │
├───────────────────────────────────────┤
│  CMSIS/硬件抽象层                    │
│  ├─ startup_stm32f407xx.s            │
│  ├─ system_stm32f4xx.c               │
│  ├─ 中断向量表                       │
│  └─ 时钟初始化                       │
├───────────────────────────────────────┤
│  STM32F407 硬件                      │
│  ├─ CPU 核心                         │
│  ├─ 外设控制器                       │
│  ├─ 内存控制器                       │
│  └─ 中断控制器                       │
└───────────────────────────────────────┘
```

### 软件版本信息

```
核心库版本：
├─ FreeRTOS Kernel      : v11.0.1 或更新
│  下载：https://github.com/FreeRTOS/FreeRTOS-Kernel
│
├─ Freemodbus           : v3.2.0 或更新
│  下载：https://github.com/cwalter-at/freemodbus
│  配置：RTU 模式，从机
│
├─ STM32F4xx Std Lib    : v2.x
│  下载：https://www.st.com/content/st_com/en.html
│
└─ CMSIS 设备包         : v2.x
   来源：STM32F4xx 设备库

编译工具：
├─ 编译器        : ARM GCC (arm-none-eabi-gcc 9.x+)
├─ 调试器        : ST-Link, J-Link
├─ IDE           : Keil MDK, IAR EWARM 或 STM32CubeIDE
└─ 烧录工具      : STM32CubeProgrammer, OpenOCD
```

---

## 核心模块说明

### 1. FreeRTOS 内核

#### 任务定义和优先级

```c
/* 任务定义 - 基于 FreeRTOS */

任务名称            优先级   周期      栈大小    功能
────────────────────────────────────────────────────────
SensorTask          3        100ms    2KB       传感器数据采集
ProcessTask         2        200ms    1.5KB     数据处理和转换
ModbusTask          4        事件驱动  2KB       Modbus 从机处理
MonitorTask         1        1000ms   1KB       系统监控和看门狗
IdleTask            0        ∞        512B      空闲任务

优先级说明：
  - 数字越大，优先级越高
  - 优先级 4：Modbus (实时性要求最高)
  - 优先级 3：传感器 (定期采集)
  - 优先级 2：数据处理 (中等)
  - 优先级 1：监控 (低优先级)
  - 优先级 0：空闲 (系统自动)
```

#### 同步原语

```c
/* FreeRTOS 同步机制 */

互斥量 (Mutex)：
  ├─ data_mutex: 保护传感器数据缓冲区
  │  用于：防止同时读写数据
  │
  └─ uart_mutex: 保护 UART 通讯
     用于：串口资源互斥

信号量 (Semaphore)：
  ├─ uart_rx_semaphore: UART 接收中断信号
  │  用于：唤醒 Modbus 处理任务
  │
  └─ sensor_ready_semaphore: 传感器数据就绪
     用于：通知处理任务数据可用

队列 (Queue)：
  ├─ sensor_data_queue: 传感器数据队列
  │  元素：8 字节 (7 个 uint16_t)
  │  深度：10 个元素
  │
  └─ log_queue: 调试日志队列 (可选)
     元素：日志字符串
     深度：20 条消息
```

### 2. Freemodbus 从机

#### 寄存器映射

```
Modbus 寄存器映射表（RTU 模式，从机地址 0x01）

寄存器类型：保持寄存器 (Holding Registers)
基地址：0x0000
数据类型：unsigned short (16-bit)
字节序：大端 (Big-Endian)

┌─────────────┬──────────────────────────┬──────┬──────────┐
│ 寄存器地址  │ 物理量                   │ 范围 │ 单位     │
├─────────────┼──────────────────────────┼──────┼──────────┤
│ 0x0000      │ AHT10 温度               │ 代码 │ 0.1°C    │
│ 0x0001      │ AHT10 湿度               │ 0-100│ %RH      │
│ 0x0002      │ BH1750 光照              │ 代码 │ lux      │
│ 0x0003      │ BMP280 温度              │ 代码 │ 0.01°C   │
│ 0x0004      │ BMP280 气压              │ 代码 │ Pa       │
│ 0x0005      │ SGP30 CO2                │ 代码 │ ppm      │
│ 0x0006      │ SGP30 TVOC               │ 代码 │ ppb      │
└─────────────┴──────────────────────────┴──────┴──────────┘

读取方式：
  Modbus 函数码 03 (Read Holding Registers)
  主机请求：
    Slave ID: 0x01
    Function: 0x03
    Start Addr: 0x0000
    Quantity: 0x0007 (读 7 个寄存器)

  从机响应：
    Slave ID: 0x01
    Function: 0x03
    Byte Count: 0x0E (14 字节 = 7 × 2)
    Data: [Reg0_H][Reg0_L][Reg1_H][Reg1_L]...[Reg6_L]

周期性轮询：
  上位机每 1 秒读取一次完整数据
  响应时间：<50ms (STM32 计算能力强)
```

#### Freemodbus 配置

```c
/* Freemodbus 配置文件 (mbconfig.h) */

MB_FUNC_HOLDING_INPUT_ENABLED   1    // 启用保持寄存器读
MB_FUNC_HOLDING_ENABLED         0    // 禁用保持寄存器写
MB_FUNC_COILS_ENABLED           0    // 禁用线圈
MB_FUNC_DISCRETE_ENABLED        0    // 禁用离散量

// 寄存器配置
MB_MASTER_TOTAL_SLAVE_ADDRESS   1    // 1 个从机
MB_MASTER_DEFAULTS_ADDRESS      1    // 从机地址 1
MB_MASTER_RTU_BUF_SIZ           256  // RTU 缓冲大小
MB_MASTER_DELAY_MS_CONVERT      0    // 延迟转换

// 通讯配置
MB_RTU_BAUDRATE                 115200
MB_RTU_PARITY                   MB_PAR_NONE  // 无奇偶校验
MB_ASCII_TIMEOUT_SEC            1    // 超时 1 秒
```

### 3. 传感器驱动

#### AHT10 温湿度传感器

```
I2C 地址：0x70 (7-bit)
工作温度：-10°C ~ 65°C
工作湿度：0% ~ 100% RH
分辨率：温度 0.1°C, 湿度 0.1% RH
精度：温度 ±0.5°C, 湿度 ±3% RH

通讯协议：
  初始化：
    1. 发送 0xBE 初始化命令
    2. 等待 10ms
    3. 读取状态字节，检查初始化完成

  读取数据：
    1. 发送 0xAC (触发测量)
    2. 发送测量参数 0x33
    3. 发送 0x00
    4. 等待 80ms (测量时间)
    5. 读取 6 字节数据
       [Byte0][Byte1][Byte2][Byte3][Byte4][Byte5]
       状态   温度(bit16-9) 温度/湿度共享 湿度(bit8-0)

驱动接口：
  aht10_init()           - 初始化传感器
  aht10_read_temp()      - 读温度 (返回 int16_t, 0.1°C)
  aht10_read_humidity()  - 读湿度 (返回 uint16_t, 0.1% RH)
  aht10_read_all()       - 读所有数据
```

#### BMP280 气压温度传感器

```
I2C 地址：0x77 (7-bit)
工作温度：-40°C ~ 85°C
工作气压：300 ~ 1100 hPa
分辨率：气压 0.01 hPa, 温度 0.01°C
精度：气压 ±1 hPa, 温度 ±0.5°C

通讯协议：
  初始化：
    1. 读芯片 ID (应为 0x58)
    2. 读补偿参数 (26 字节)
    3. 设置工作模式和过采样配置

  读取数据：
    1. 读取状态寄存器 (0xF3)
    2. 读取 3 字节温度原始值 (0xFA-0xFC)
    3. 读取 3 字节气压原始值 (0xF7-0xF9)
    4. 使用补偿参数计算实际值

驱动接口：
  bmp280_init()          - 初始化传感器
  bmp280_read_temp()     - 读温度 (返回 int16_t, 0.01°C)
  bmp280_read_pressure() - 读气压 (返回 uint32_t, Pa)
  bmp280_read_all()      - 读所有数据
  bmp280_calc_altitude() - 根据气压计算海拔
```

#### SGP30 空气质量传感器

```
I2C 地址：0x58 (7-bit)
测量参数：CO2 等效浓度 (eCO2), 总挥发有机物 (TVOC)
范围：eCO2: 400-8192 ppm, TVOC: 0-60000 ppb
精度：±15% (对于非常低的值) 或 ±10% (一般情况)

通讯协议：
  初始化：
    1. 获取芯片序列号和产品类型
    2. 初始化传感器 (iaq_init)
    3. 等待 15ms

  读取数据：
    1. 发送 0x2008 (measure_air_quality 命令)
    2. 等待 12ms (测量时间)
    3. 读取 3 字节数据 (CO2)
    4. 读取 3 字节数据 (TVOC)
    5. 读取 1 字节校验和

驱动接口：
  sgp30_init()           - 初始化传感器
  sgp30_read_co2()       - 读 eCO2 (返回 uint16_t, ppm)
  sgp30_read_tvoc()      - 读 TVOC (返回 uint16_t, ppb)
  sgp30_read_all()       - 读所有数据
  sgp30_set_humidity()   - 设置湿度补偿
```

### 4. UART Modbus 通讯

#### 中断驱动实现

```c
/* UART1 Modbus 配置 */

波特率：     115200 bps
数据位：     8
停止位：     1
奇偶校验：   无
流控制：     无

中断处理：
  USART1_IRQHandler()
    ├─ RXNE (接收缓冲非空)
    │  └─ 调用 pxMBFrameCBByteReceived() 字节接收回调
    │
    └─ TXE (发送缓冲空)
       └─ 调用 pxMBFrameCBTransmitterEmpty() 字节发送回调

DMA 配置 (可选，高性能方案)：
  RX DMA：
    └─ 通道 4, 优先级高
       单次传输最多 256 字节
       触发中断时处理一帧

  TX DMA：
    └─ 通道 7, 优先级高
       传输响应数据
       完成中断触发下一帧处理
```

---

## 数据流和通信

### 通信时序

```
时间    上位机 (Master)          下位机 (Slave)       说明
────────────────────────────────────────────────────────────
T=0s    ┌─────────┐
        │ 发送请求 │────────────→  接收请求
        │ Modbus  │             解析命令
        │ Read Reg│             准备响应
        └─────────┘
          ↓  (100ms)              ↓
T=0.05s   等待                   ┌──────────────┐
                                 │ 生成响应数据  │
                                 │ 寄存器 0-6   │
                                 │ CRC 校验和   │
                                 └──────────────┘
          ↓  (50ms 往返延迟)          ↓
T=0.10s   ┌─────────┐             ┌─────────┐
          │ 接收响应 │←────────────│ 发送响应 │
          │ 解析数据 │             │ 完成    │
          │ 显示 UI  │             └─────────┘
          └─────────┘

循环周期：1 秒（10 次读取）
  ├─ T=0.00-0.10s: 第 1 次读取
  ├─ T=0.10-0.20s: 第 2 次读取
  ├─ ...
  └─ T=0.90-1.00s: 第 10 次读取
```

### 数据帧格式

```
Modbus RTU 数据帧格式：

请求帧 (Master → Slave)：
┌─────┬──────────┬────────────┬─────────────┬────────────┐
│ 地址 │ 功能码  │ 寄存器地址  │ 寄存器数量  │ CRC 校验   │
│ 1字节│ 1字节   │ 2字节      │ 2字节       │ 2字节      │
│ 0x01 │ 0x03    │ 0x0000     │ 0x0007      │ CRC16      │
└─────┴──────────┴────────────┴─────────────┴────────────┘
总计：8 字节

功能码 0x03: Read Holding Registers
  - 寄存器地址 0x0000: 起始地址
  - 数量 0x0007: 读 7 个寄存器 (0-6)

响应帧 (Slave → Master)：
┌─────┬──────────┬──────────┬────────────────────┬────────────┐
│ 地址 │ 功能码  │ 字节数  │ 寄存器数据          │ CRC 校验   │
│ 1字节│ 1字节   │ 1字节   │ 14字节 (7×2)       │ 2字节      │
│ 0x01 │ 0x03    │ 0x0E    │ Reg0-Reg6 数据     │ CRC16      │
└─────┴──────────┴──────────┴────────────────────┴────────────┘
总计：20 字节

CRC-16 校验：
  多项式：0xA001
  初始值：0xFFFF
  算法：标准 Modbus CRC-16-IBM
```

### 数据在寄存器中的表示

```
物理数据 → 寄存器转换示例：

温度：25.5°C
  ├─ 原始值：255 (代码值)
  ├─ 寄存器值：uint16_t = 0x00FF
  ├─ 上位机换算：255 × 0.1 = 25.5°C
  └─ 优点：节省通讯数据量，易于扩展精度

湿度：65.3% RH
  ├─ 原始值：653 (代码值)
  ├─ 寄存器值：uint16_t = 0x028D
  ├─ 上位机换算：653 × 0.1 = 65.3% RH
  └─ 范围检查：0-1000 代表 0-100% (分辨率 0.1%)

气压：1013.25 hPa
  ├─ 原始值：101325 (Pa)
  ├─ 寄存器值：uint16_t = 0xE369 (仅用低 16 位，实际使用扩展)
  ├─ 上位机换算：(寄存器值 × 10) = 101325 Pa = 1013.25 hPa
  └─ 或者：直接传输 hPa × 10 作为整数
```

---

## 传感器驱动

### I2C 驱动架构

```c
/* I2C1 传感器总线配置 */

I2C 时钟频率：400kHz (快速模式)
工作电压：3.3V
上拉电阻：4.7kΩ (标准值)

设备地址：
  ├─ AHT10: 0x70 (写操作) / 0x71 (读操作)
  ├─ BMP280: 0x77 (可选 0x76)
  └─ SGP30: 0x58 (写操作) / 0x59 (读操作)

I2C 工作流程：
  ┌──────────────────────┐
  │ I2C 初始化           │
  │ - 启用 I2C1          │
  │ - 设置时钟和引脚     │
  │ - 配置中断           │
  └──────────────────────┘
        │
        ↓
  ┌──────────────────────┐
  │ 传感器初始化         │
  │ - 发送初始化命令     │
  │ - 读取配置参数       │
  │ - 验证芯片 ID        │
  └──────────────────────┘
        │
        ↓
  ┌──────────────────────┐
  │ 定期读取数据         │
  │ - 发送测量命令       │
  │ - 等待测量完成       │
  │ - 读取原始数据       │
  │ - 数据处理和转换     │
  │ - 存储到寄存器       │
  └──────────────────────┘

错误处理：
  ├─ 总线错误：自动复位 I2C
  ├─ 设备未响应：标记为离线
  ├─ 数据校验失败：丢弃该次数据
  └─ 重试机制：最多重试 3 次
```

### 驱动集成

```
传感器驱动层次结构：

┌─────────────────────────────────────┐
│ 应用层 (SensorTask)                 │
│ - 周期性调用传感器读取              │
│ - 处理读取结果                      │
│ - 更新 Modbus 寄存器               │
└────────────┬────────────────────────┘
             │
        ↓
┌─────────────────────────────────────┐
│ 传感器驱动层                        │
│ ├─ aht10.c/h   (AHT10 驱动)        │
│ ├─ bmp280.c/h  (BMP280 驱动)       │
│ ├─ sgp30.c/h   (SGP30 驱动)        │
│ └─ sensor_common.c/h (通用接口)    │
└────────────┬────────────────────────┘
             │
        ↓
┌─────────────────────────────────────┐
│ I2C HAL 驱动                        │
│ ├─ HAL_I2C_Master_Receive()         │
│ ├─ HAL_I2C_Master_Transmit()        │
│ ├─ HAL_I2C_IsDeviceReady()          │
│ └─ 中断处理回调                     │
└────────────┬────────────────────────┘
             │
        ↓
┌─────────────────────────────────────┐
│ STM32 硬件 (I2C1)                  │
│ ├─ I2C 控制器                      │
│ ├─ 引脚 (PB6-SDA, PB7-SCL)         │
│ └─ 时钟生成                        │
└─────────────────────────────────────┘
```

---

## 任务调度

### FreeRTOS 任务模型

```
任务状态转移图：

        ┌─────────────┐
        │   Ready     │ ← 就绪态 (等待 CPU 分配)
        └─────────────┘
             ↑ │
             │ ↓ (CPU 分配)
        ┌─────────────┐
        │   Running   │ ← 运行态 (正在执行)
        └─────────────┘
             │
    ┌────────┼────────┐
    ↓        │        ↓
Blocked  Suspended Running
(阻塞)    (挂起)
    ↑        │        ↑
    └────────┼────────┘
             │
        (中断/事件)

STM32F407 任务调度：

┌─────────────────────────────────────────────────────┐
│ Scheduler (优先级抢占式)                            │
├─────────────────────────────────────────────────────┤
│                                                     │
│ 优先级 4  ┌──────────────────────┐                 │
│ (ModbusT) │ 等待 UART 中断激活    │ (事件驱动)      │
│           └──────────────────────┘                 │
│                                                     │
│ 优先级 3  ┌──────────────────────┐                 │
│ (SensorT) │ 100ms 周期激活        │ (定时激活)      │
│           │ 读传感器数据          │                 │
│           └──────────────────────┘                 │
│                                                     │
│ 优先级 2  ┌──────────────────────┐                 │
│ (ProcessT)│ 200ms 周期激活        │ (定时激活)      │
│           │ 处理传感器数据        │                 │
│           └──────────────────────┘                 │
│                                                     │
│ 优先级 1  ┌──────────────────────┐                 │
│ (MonitorT)│ 1000ms 周期激活       │ (定时激活)      │
│           │ 系统监控              │                 │
│           └──────────────────────┘                 │
│                                                     │
│ 优先级 0  ┌──────────────────────┐                 │
│ (Idle)    │ 空闲任务              │ (始终存在)      │
│           └──────────────────────┘                 │
│                                                     │
└─────────────────────────────────────────────────────┘

任务切换延迟：
  - SysTick 中断周期：1ms
  - 最大上下文切换时间：<100μs (STM32F407 @168MHz)
  - 整体响应延迟：<150μs (对 Modbus 请求)
```

### 数据同步机制

```
传感器采集 → 数据处理 → Modbus 寄存器 的流程：

Step 1: SensorTask (优先级 3, 每 100ms 执行一次)
  ┌─────────────────────────────┐
  │ for each sensor:             │
  │   - i2c_read(sensor)         │
  │   - store raw_data[]         │
  │   - signal semaphore         │
  │ mutex_lock(data_mutex)       │
  │ copy to sensor_buffer        │
  │ mutex_unlock(data_mutex)     │
  └─────────────────────────────┘
        ↓ (signal)

Step 2: ProcessTask (优先级 2, 200ms 周期或信号激活)
  ┌─────────────────────────────┐
  │ wait_semaphore()             │
  │ mutex_lock(data_mutex)       │
  │ for each sensor_value:       │
  │   - apply_filter()           │
  │   - convert_units()          │
  │   - validate_range()         │
  │   - update_modbus_reg()      │
  │ mutex_unlock(data_mutex)     │
  └─────────────────────────────┘
        ↓ (Modbus 寄存器更新)

Step 3: ModbusTask (优先级 4, 事件驱动)
  ┌─────────────────────────────┐
  │ wait for UART RX interrupt   │
  │ parse_modbus_request()       │
  │ mutex_lock(data_mutex)       │
  │ read_modbus_registers()      │
  │ mutex_unlock(data_mutex)     │
  │ build_response()             │
  │ send_via_uart()              │
  └─────────────────────────────┘

同步保护：
  ├─ data_mutex: 数据缓冲区互斥访问
  ├─ uart_mutex: 串口独占访问
  ├─ 信号量: 任务间协调
  └─ 队列: 可选的异步通讯
```

---

## 资源和性能

### 内存使用统计

```
Flash 内存分配（1MB 总容量）：

使用项目                    大小        比例
─────────────────────────────────────────
启动代码 (startup)          32KB       3.1%
FreeRTOS 内核               60KB       5.9%
应用代码 (main, tasks)      150KB      14.6%
Freemodbus 库               80KB       7.8%
传感器驱动 (AHT10/BMP280)   50KB       4.9%
STM32 HAL 库                120KB      11.7%
其他库和工具函数            100KB      9.8%
─────────────────────────────────────────
已使用                      592KB      57.8%
剩余/保留                   408KB      39.8%
引导加载程序                0KB        0.0%

SRAM 内存分配（192KB 总容量）：

使用项目                    大小        说明
─────────────────────────────────────────
FreeRTOS 堆                 80KB       任务创建、对象分配
任务栈                      40KB       5个任务 × 8KB
  ├─ SensorTask             8KB
  ├─ ProcessTask            8KB
  ├─ ModbusTask             8KB
  ├─ MonitorTask            8KB
  ├─ IdleTask               8KB

传感器缓冲                  10KB       原始数据存储
Modbus 通讯缓冲             8KB        请求/响应帧缓冲
全局数据结构                20KB       变量、结构体
中断栈                      4KB        中断处理栈
其他动态分配                30KB       临时数据、队列等
─────────────────────────────────────────
总使用                      192KB      100%
可用余量                    0KB        (紧张)

优化建议：
  ├─ 减少任务栈大小：可能导致栈溢出，不推荐
  ├─ 使用 static 分配替代动态分配
  ├─ 优化数据结构，减少冗余
  └─ 考虑压缩传感器缓冲（环形缓冲）
```

### 性能指标

```
实时性指标：

任务名称          执行时间    优先级    周期      抖动
─────────────────────────────────────────────────────
SensorTask        5-15ms      3        100ms    ±5ms
ProcessTask       10-20ms     2        200ms    ±10ms
ModbusTask        2-5ms       4        事件驱动  ±1ms
MonitorTask       2-3ms       1        1000ms   ±50ms

中断延迟：
  ├─ SysTick (系统定时): 1ms 中断一次
  ├─ UART RX (接收): <10μs 响应延迟
  ├─ UART TX (发送): <10μs 完成中断
  └─ 最大中断禁用时间: <100μs

通讯性能：
  ├─ Modbus 请求处理: <50ms (从接收到响应发出)
  ├─ 传感器 I2C 读取: 20-50ms (取决于传感器)
  ├─ 数据转换处理: 1-5ms
  └─ 单次轮询周期: <100ms

CPU 占用率：
  ├─ 空闲时: ~30% (等待中断或 sleep)
  ├─ 正常运行: ~50-60%
  ├─ 峰值: ~80% (所有任务同时执行)
  └─ 裕度: 20-40% (安全缓冲)
```

---

## 开发指南

### 编译和烧录

```bash
# 编译 (Keil MDK 或 ARM GCC)

# 使用 ARM GCC (推荐开源方案)
arm-none-eabi-gcc -mcpu=cortex-m4 -mthumb \
  -DSTM32F407xx \
  -I./Inc \
  -I./Drivers/CMSIS \
  -I./Drivers/STM32F4xx_StdPeriph_Driver/inc \
  -I./FreeRTOS/include \
  -I./Freemodbus \
  -c Src/main.c -o main.o

# 链接
arm-none-eabi-ld -T ./STM32F407ZGTx_FLASH.ld ...

# 生成 .hex 或 .bin
arm-none-eabi-objcopy -O ihex main.elf main.hex
arm-none-eabi-objcopy -O binary main.elf main.bin

# 烧录 (ST-Link)
STM32_Programmer_CLI -c port=SWD -w main.bin 0x08000000
# 或使用 OpenOCD:
openocd -f interface/stlink.cfg -f target/stm32f4x.cfg \
  -c "program main.bin verify reset 0x08000000"
```

### 调试方法

```c
/* 调试技巧 */

1. 串口调试输出：
   printf("Sensor data: Temp=%d.%d, Humid=%d\n",
          temp/10, temp%10, humidity);
   // 配置串口重定向 printf

2. LED 状态指示：
   LED1: 系统运行指示灯 (1s 闪烁)
   LED2: Modbus 活动指示 (有请求时亮)
   LED3: 错误指示灯 (错误时亮)

3. 实时观察器 (IDE 支持)：
   ├─ 监控全局变量
   ├─ 查看任务状态
   ├─ 观察 Modbus 寄存器值
   └─ 分析堆栈情况

4. 逻辑分析仪测量：
   ├─ UART 波形验证 (115200)
   ├─ I2C 总线分析
   └─ GPIO 信号时序
```

### 常见问题

```
Q1: Modbus 请求无响应
A: 检查项：
   ├─ UART 连接和波特率
   ├─ Freemodbus 中断配置
   ├─ 寄存器地址映射
   └─ CRC 校验计算

Q2: 传感器数据异常
A: 排查步骤：
   ├─ I2C 总线连接
   ├─ 传感器初始化代码
   ├─ 数据校验和转换
   └─ 单独测试各传感器

Q3: 内存溢出/栈溢出
A: 解决方案：
   ├─ 减少缓冲区大小
   ├─ 优化堆分配
   ├─ 增加栈大小 (可能需要增加 SRAM)
   └─ 使用内存映射工具分析

Q4: 任务优先级冲突
A: 解决方法：
   ├─ 使用 FreeRTOS 自带的 vTaskList()
   ├─ 调整任务优先级
   ├─ 检查优先级反转问题
   └─ 使用互斥量而不是信号量
```

---

## 总结

Remote-Env-Monitor 下位机是一个完整的实时嵌入式系统，集成了：

✓ **实时操作系统**：FreeRTOS 为多任务提供抢占式调度
✓ **通讯协议**：Freemodbus RTU 从机实现标准 Modbus 通讯
✓ **硬件驱动**：STM32F4xx 标准外设库支持所有必需功能
✓ **传感器支持**：多个 I2C 传感器驱动完整集成
✓ **数据同步**：互斥量和信号量确保线程安全
✓ **性能优化**：中断驱动、DMA 加速、事件驱动模型

下位机与上位机通过 Modbus RTU 协议紧密协作，实现了 Remote-Env-Monitor 的完整功能。

---

**文档版本：** 1.0
**最后更新：** 2026年2月18日
**维护者：** Lenmoncc + Claude AI
**适用范围：** Remote-Env-Monitor 下位机 (STM32F407ZGT6 + FreeRTOS + Freemodbus)
